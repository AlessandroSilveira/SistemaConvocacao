using System;
using System.Text;

namespace SisConv.Domain.Core.Services.DecriptarSenha
{
	public class DecriptarSenha : IDecriptarSenha
	{
		//public string DecriptadorSenha(string senha)
		//{
		//	var encodedPassword = Convert.FromBase64String(encryptedPassword);
		//	var bytes = base.DecryptPassword(encodedPassword);
		//	return Encoding.Unicode.GetString(bytes, 0x10, bytes.Length - 0x10);
		//}
	}
}
