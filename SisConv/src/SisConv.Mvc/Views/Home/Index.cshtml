@{
	ViewBag.Title = "Home Page";
}

<div class="col-lg-12">
	@if (Request.IsAuthenticated && User.IsInRole("Administrator"))
	{
		<table class="table table-striped table-hover ">
			<tbody>
				<tr>
					<td>
						@Html.ActionLink("Cadastro e Gerenciamento de Clientes", "Index", "Cliente")
					</td>
				</tr>
				<tr>
					<td>
						@Html.ActionLink("Cadastro e Gerenciamento de Usuários", "Index", "UsersAdmin")
					</td>
				</tr>
			</tbody>
		</table>
	}
	@if (Request.IsAuthenticated && User.IsInRole("Cliente"))
	{
		<table class="table table-striped table-hover ">
			<tbody>
				<tr>
					<td>
						@Html.ActionLink("Cadastro e Gerenciamento de Convocações", "Index", "Processos")
					</td>
				</tr>
			</tbody>
		</table>
	}
	@if (Request.IsAuthenticated && User.IsInRole("Convocado"))
	{
		using (Html.BeginForm("ConfirmaConvocacao", "Convocacao", FormMethod.Post, new { @id = "form" }))
		{
			<div class="container">
				<div class="form-horizontal">
					<h4></h4>
					<h4></h4>
					<div class="jumbotron">
						<h1>Bem Vindo @ViewBag.dadosConvocado.Nome</h1>
						<p>@ViewBag.dadosProcesso.TextoInicialTelaConvocado</p>
						<p>Prezado Candidato, você foi convocado para o @ViewBag.dadosProcesso.Nome </p>
						<p>No dia: @ViewBag.dadosConvocacao.DataEntregaDocumentos.ToString("dd/MM/yyyy"), às @ViewBag.dadosConvocacao.HorarioEntregaDocumento</p>
						<p>No endereço: @ViewBag.dadosConvocacao.EnderecoEntregaDocumento</p>
						<p>
							Você aceita participar dessa convocação?
						</p>
						<p>
							<input type="hidden" name="ProcessoId" value="@ViewBag.dadosProcesso.ProcessoId" />
							<input type="hidden" name="ConvocadoId" value="@ViewBag.dadosConvocacao.ConvocadoId" />
							<input type="hidden" name="ConvocacaoId" value="@ViewBag.dadosConvocacao.ConvocacaoId" />
							<input type="radio" name="decisao" value="S" /> SIM, DESEJO PARTICIPAR
							<input type="radio" name="decisao" value="N" /> NÃO DESEJO PARTICIPAR
						</p>
						<p>
							<input type="submit" value="Enviar" />
						</p>
					</div>
				</div>
			</div>
		}
	}
</div>


