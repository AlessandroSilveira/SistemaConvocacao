
@{
	ViewBag.Title = "AtualizarCandidatosConfirmados";
}

<h2>AtualizarCandidatosConfirmados</h2>
@foreach (var itens in ViewBag.ListaCandidatos)
{
	foreach (var result in itens)
	{
		@result.Conv.Nome
	}
}